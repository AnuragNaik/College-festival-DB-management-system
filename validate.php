<?php

$error = ""; 
 
if (isset($_POST['submit'])) { 
	/*$name1 = trim($_POST['name']);
	$uname1 = trim($_POST['user']);
	$pass1 = $_POST['pass'];
	$cpass1 = $_POST['cpass'];
	$cnum1 = $_POST['pnum'];
	$email1 = $_POST['email'];
	
	if(empty($name1))
    {
        $error .= '<p class="error">Name can not be empty.</p>';
    }
     
    if(empty($uname1))
    {
        $error .= '<p class="error">Username can not be empty.</p>';
    }
    
    if(empty($pass1) || empty($cpass1))
    {
        $error .= '<p class="error">Password can not be empty.</p>';
    }
    
    
	
	if (!ctype_alpha(str_replace(array(" ","'", "-"), "",$name1))) { 
			$error .= '<p class="error">First name should be alpha characters only.</p>';
		}
	
	if (!ctype_alnum($uname1)) {
			$error .= '<p class="error">Username should be alpha numeric characters only.</p>';
		}
		
	if ($cpass1 != $pass1) {
			$error .= '<p class="error">Confirm password doesnt match.</p>';
		}

	if (!ctype_digit($cnum1) OR strlen($cnum1) != 10) {
			$error .= '<p class="error">Enter a valid 10 digit phone number.</p>';
		}
		
	if (!filter_var($email1, FILTER_VALIDATE_EMAIL)) { 
			$error .= '<p class="error">Enter a valid email address.</p>';
		}
	*/

}


?>
